#!/bin/bash
# Quick command to ask creators a question
# Usage: ./ask "How do I overcome fear?"
# Usage: ./ask "Should I start a business?" --creator Tim

SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
python3 "$SCRIPT_DIR/content_monitor.py" --ask "$@"
